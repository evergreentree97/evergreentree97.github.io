# Portfolio Project Context

공개 포트폴리오 사이트다. 구현과 배포 방법은 `README.md`를 따른다.

## 콘텐츠 원칙

- 역할과 판단, 확인 가능한 결과를 중심으로 쓴다.
- 제품 문제와 판단 범위를 먼저 제시하고 Android와 LLM은 이를 증명하는 담당 경험으로 연결한다. 두 기술 영역을 별개의 경력이나 기술 목록처럼 나열하지 않는다.
- `Android Developer`는 공식 경력 직무이고, `AI-Native Product Engineer`는 제품 문제에 따라 Android와 LLM까지 연결한 현재 포트폴리오의 포지셔닝이다.
- 주요 프로젝트처럼 제품 판단을 증명하는 섹션을 도구와 작업 절차보다 먼저 보여준다.
- 연락 문구는 Android 채용으로 범위를 제한하지 않고 채용과 협업 제안을 모두 포괄한다.
- 공개가 허용된 경력 정보만 사용한다.
- 개인 연락처, 원본 이력서, 사내 코드명과 비공개 제품 정보는 저장하지 않는다.
- 수치에는 표본과 비교 조건을 함께 적고 불리한 결과를 숨기지 않는다.

## 문체와 검증

- 짧고 자연스러운 한국어를 사용한다.
- 메인 카피를 제외한 설명은 문어체로 작성하고, 구어체 축약이나 명사만 남은 표어 대신 실제 행동과 판단을 적는다.
- 자명한 문서 유형을 소개 문장에 반복하지 않는다. 메타 설명도 `포트폴리오입니다`로 끝내지 않고 역할과 판단 범위만 전달한다.
- 병렬 정보와 절차는 글머리기호나 카드로 구분한다. 긴 문단으로 합치지 않는다.
- 작업 절차의 단계명은 Review, Isolate, Diagnose, Fix, Verify처럼 짧은 영어 키워드를 쓰고 설명은 자연스러운 한국어로 작성한다.
- AI 작업 맥락의 코드베이스는 `Repository`, 문서화한 반복 절차는 `Repository Skill`로 표기한다.
- `접수`, `코드는 직접 봄`, `화면을 증거로 남김`처럼 관료적이거나 구어적인 표현을 피하고, 검토 대상과 검증 방법을 구체적으로 적는다.
- 소프트웨어 변경 검증을 `회귀 테스트`로 부르지 않는다. `기존 기능 테스트`, `수정 전후 동작 확인` 또는 실제로 확인한 동작을 쓴다.
- 번역투, 과장된 표현과 설명 없는 내부 용어를 피한다.
- 가운뎃점 문자는 사용하지 않는다. 쉼표나 `과`, `와`처럼 문맥에 맞는 표현으로 풀어 쓴다.
- 수정 후 `npm run check`를 실행한다.
