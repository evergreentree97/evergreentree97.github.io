# Ricky Choi Portfolio Context

Android 제품 경험을 기반으로 AI Native 제품 개발 방식까지 보여주는 커리어 포트폴리오다. 구현과 배포 정보는 `README.md`를 따른다.

## 방향

- Android와 AI를 별개 경력처럼 나누지 말고 제품을 만들고 검증하는 하나의 흐름으로 보여준다.
- 기술 목록보다 채용 담당자가 이해할 수 있는 역할, 판단, 결과를 먼저 쓴다.
- AI 캐릭터와 사내 코드명처럼 공개하기 어려운 정보는 드러내지 않는다.
- AI Native 업무 방식은 저장소 맥락 공유, 확인 가능한 작업 범위, 직접 코드 리뷰, 실제 테스트를 중심으로 설명한다.

## 문체

- 짧고 자연스러운 한국어를 사용한다. 가능한 곳은 명사형으로 끝낸다.
- `했습니다`의 반복, 번역투, 과장된 대표 문구를 피한다.
- 가운데점 문자와 em dash를 사용하지 않는다.
- 설명 없이 이해하기 어려운 내부 용어나 AI 업계 표현을 쓰지 않는다.

## 확인된 평가 수치

- 핵심 질문 40건 기준 종합 품질 점수 6.9% 상승
- 치명적 실패 4건에서 0건으로 감소
- 제품 규칙 위반 4건에서 1건으로 감소

## 다음 문구 검토

- `src/components/ProofStrip.tsx`의 평가 근거 문구는 현재 어색하므로 새 관점에서 다시 작성한다.
- `src/data/workingMethod.ts`의 `ASSIGN` 제목과 설명도 현재 문구에 얽매이지 말고 다시 작성한다.

수정 후 `npm run check`를 실행한다. `main`에 push하면 GitHub Pages에 자동 배포된다.
